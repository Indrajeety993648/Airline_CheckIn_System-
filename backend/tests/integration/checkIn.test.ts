
describe('Check In API', () => {
  it('should check in a passenger', async () => {
    // const res = await request(app).post('/api/check-in').send({ pnr: '123456', seat: '1A' });
    // expect(res.statusCode).toEqual(200);
    expect(true).toBe(true);
  });
});
